# @package algorithm


# Override loss type to use HAPPO


# HAPPO typically benefits from slightly different hyperparameters
loss:
  loss_type: ppo
  clip_epsilon: 0.2
  entropy_coeff: 0.01
  critic_coeff: 1.0
  gamma: 0.99
  lmbda: 0.95
  normalize_advantage: true
