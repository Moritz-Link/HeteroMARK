# Loss function configuration
# Parameters for PPO/HAPPO loss computation

clip_epsilon: 0.2  # PPO clipping parameter
entropy_coeff: 0.01  # Entropy bonus coefficient for exploration
critic_coeff: 1.0  # Critic loss weight
gamma: 0.99  # Discount factor for rewards
lmbda: 0.95  # GAE lambda parameter for advantage estimation
normalize_advantage: true  # Whether to normalize advantages
